{
	"$schema": "https://turbo.build/schema.json",
	"pipeline": {
		"build": {
			"dependson": ["^build"],
			"outputs": [".svelte-kit/**", ".vercel/**"]
		},
		"lint": {},
		"dev": {
			"cache": false,
			"persistent": true
		}
	},
	"globalEnv": ["DATABASE_URL", "JWT_SECRET"]
}
